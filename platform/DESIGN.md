# Platform Design Mirror

The canonical design document currently lives at the repository root:

- [`../DESIGN.md`](../DESIGN.md)

This file exists so the new `platform/` repository matches the directory contract defined in the design.
