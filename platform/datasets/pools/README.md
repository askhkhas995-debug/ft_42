# Pools

Canonical pool bundles live under `datasets/pools/<track>/<slug>/pool.yml`.

Pool datasets define:

- level structure
- selection strategy
- repeat policy
- unlock rules
- timing and cooldown metadata

Pool entries reference canonical `exercise_id` values resolved through the catalog.
